VITE_SUPABASE_PROJECT_ID="your-project-ref"
VITE_SUPABASE_URL="https://your-project-ref.supabase.co"
VITE_SUPABASE_PUBLISHABLE_KEY="your-publishable-key"

# Supabase Edge Function secrets are configured in Supabase, not in this frontend .env:
# REPLICATE_API_TOKEN
# KIOSK_SIMULATE_PAYMENTS=true only for lab tests without PagBank
# PAGBANK_API_TOKEN only after PagBank approval
